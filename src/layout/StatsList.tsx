import React from 'react';
import {styled} from "styled-components";

type PropsType = {

}

export const StatsList = (props: PropsType) => {
    return(
        <StyledStatsList>

        </StyledStatsList>
    )
}

const StyledStatsList = styled.div`
`